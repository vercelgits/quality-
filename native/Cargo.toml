[package]
name = "orbit-natif"
version = "0.1.0"
edition = "2021"
description = "Orbit — client de bureau natif, sans moteur web"

[dependencies]
slint = "1.17"

[build-dependencies]
slint-build = "1.17"

[profile.release]
opt-level = "z"
lto = true
codegen-units = 1
strip = true
panic = "abort"
